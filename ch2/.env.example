# Chapter 2 — Bitcoin one-hop trace agent
# Required only when running with --ai. Copy to .env; do not commit .env.

# Prefer either key name (script accepts both):
OPENAI_API_KEY=sk-your-key-here
# OPENAI_API_KEY_VF=sk-your-key-here

# Optional model override (default: gpt-5)
# OPENAI_MODEL=gpt-5
